import styled from 'styled-components'

export const Container = styled.div`
  
  margin-top: 30px;
  border-radius: 10px;
  background-color: white;

  #bar-chart {
    overflow-y: hidden;
    overflow-x: scroll;
    margin: 0 auto;
    max-width: 600px;
    height: 400px
  }
`