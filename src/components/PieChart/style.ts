import styled from 'styled-components'

export const Container = styled.div`
  border-radius: 10px;
  background-color: white;

  #pie-chart {
    overflow-y: hidden;
    overflow-x: scroll;
    margin: 0 auto;
    max-width: 500px;
  }
`